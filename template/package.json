{
    "name": "my-automation-tests",
    "version": "1.0.0",
    "description": "Automation test project using please-test",
    "scripts": {
        "test": "playwright test",
        "report": "playwright test && node scripts/open-report.js"
    },
    "dependencies": {
        "please-test": "latest"
    },
    "devDependencies": {
        "dotenv": "^16.0.0",
        "@playwright/test": "^1.40.0"
    }
}
